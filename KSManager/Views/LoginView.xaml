<UserControl x:Class="KSManager.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KSManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">

            <Label Content="Username"></Label>
            <TextBox x:Name="Username" Margin="0,5,0,0"></TextBox>

            <Label Content="Password"/>
            <TextBox x:Name="Password" Margin="0,5,0,0"></TextBox>

            <Button x:Name="Login" Content="Login" Margin="0,5,0,0"></Button>


</StackPanel>
            
    </Grid>
</UserControl>
